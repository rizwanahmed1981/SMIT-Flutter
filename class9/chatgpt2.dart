/*Find Maximum:

Create a list of numbers, manually find the largest number using only if-else and List methods.
*/

void main(){
  List<int> numbers = [1,8,6,9,7,21,56,89,45];
  int max =0;

  if(numbers[1]> max){
    max = numbers[1];
  }
  if(numbers[2] > max){
    max =numbers[2];
  }
  if(numbers[3]> max){
    max = numbers[3];
  }
  if(numbers[4]> max){
    max = numbers[4];
  }
  if(numbers[5]> max){
    max = numbers[5];
  }
  if(numbers[6]> max){
    max = numbers[6];
  }
  if(numbers[7]> max){
    max = numbers[7];
  }
  if(numbers[8]> max){
    max = numbers[8];
  }
  print("maximum number is $max");
}